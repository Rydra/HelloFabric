<?xml version="1.0" encoding="utf-8"?>
<ServiceManifest Name="WebApiPkg"
                 Version="1.0.0"
                 xmlns="http://schemas.microsoft.com/2011/01/fabric"
                 xmlns:xsd="http://www.w3.org/2001/XMLSchema"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <ServiceTypes>
    <!-- Este es el nombre del tipo de servicio. 
         Este nombre debe coincidir con la cadena usada en la llamada a RegisterServiceType en Program.cs. -->
    <StatelessServiceType ServiceTypeName="WebApiType" />
  </ServiceTypes>

  <!-- El paquete de código es el archivo ejecutable del servicio. -->
  <CodePackage Name="Code" Version="1.0.0">
    <EntryPoint>
      <ExeHost>
        <Program>WebApi.exe</Program>
      </ExeHost>
    </EntryPoint>
  </CodePackage>

  <!-- El paquete de configuración es el contenido del directorio Config de PackageRoot, que contiene 
       un conjunto de valores de configuración personalizados para el servicio que tiene número de versión y que se puede actualizar de manera independiente. -->
  <ConfigPackage Name="Config" Version="1.0.0" />

  <Resources>
    <Endpoints>
      <!-- El agente de escucha de comunicación utiliza este punto de conexión para obtener el puerto 
           en el que debe escuchar. Tenga en cuenta que, si el servicio está particionado, este puerto se comparte con 
           réplicas de diferentes particiones que se ponen en el código. -->
      <Endpoint Protocol="http" Name="ServiceEndpoint" Type="Input" Port="8204" />
    </Endpoints>
  </Resources>
</ServiceManifest>